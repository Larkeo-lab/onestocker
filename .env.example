# base URL ของ Go (Fiber) API — ไม่ต้องมี / ปิดท้าย
VITE_API_URL=http://localhost:8080/api

# publishable key ของ Clerk (เป็น public โดยตั้งใจ)
# secret key อยู่ฝั่ง Go เท่านั้น ห้ามใส่ที่นี่
VITE_CLERK_PUBLISHABLE_KEY=
